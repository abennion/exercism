{
  "name": "wordy",
  "version": "1.0.0",
  "description": "Write a program that takes a word problem and returns the answer as an integer.",
  "main": "wordy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^3.1.1",
    "eslint-config-google": "^0.6.0"
  }
}
