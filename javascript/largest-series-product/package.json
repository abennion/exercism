{
  "name": "largest-series-product",
  "version": "1.0.0",
  "description": "Write a program that, when given a string of digits, can calculate the largest product for a contiguous substring of digits of length n.",
  "main": "largest-series-product.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^3.1.1",
    "eslint-config-google": "^0.6.0"
  }
}
